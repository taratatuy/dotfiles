" NetRW:

let g:netrw_altv=1 " Vertical open file to the right via NetRW
let g:netrw_preview=1 " Preview to the right via netRW
let g:netrw_winsize=20 " Set equal windows size when open new window
let g:netrw_banner=0 " Turn off the banner on the top
let g:netrw_liststyle=3 " Tree listing of files
" let g:netrw_browse_split = 4 " Use the previous window to open file
let g:netrw_sort_sequence = '[\/]$,*' " Sort is affecting only: directories on the top, files below

